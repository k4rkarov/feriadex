/**
 * Public site URL (origin + base path). Override with NEXT_PUBLIC_SITE_URL when
 * moving to a custom domain. Used for canonical/OG/sitemap absolute URLs.
 */
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://k4rkarov.github.io/feriadex";
